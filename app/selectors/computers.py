from app.core.logger import logger

class Computers:
    logger.info(__name__)
    search_box = '#searchbox'
    filter_by_name = '#searchsubmit'
    add_new_computer = '#add'
    alert_message = '.alert-message.alert-message'
